import * as React from 'react';

const HowIT = ({ id }) => {
    return (
        <div style={{ alignItems: "center", justifyContent: "center", display: "flex", height: "500px" }} id={id}>
            <h1>
                How It Works
            </h1>
        </div >
    );
};
export default HowIT;
