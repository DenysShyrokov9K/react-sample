import * as React from 'react';

const Account = ({ id }) => {
    return (
        <div style={{ alignItems: "center", justifyContent: "center", display: "flex", height: "1000px" }} id={id}>
            <h1>
                Account
            </h1>
        </div >
    );
};
export default Account;
