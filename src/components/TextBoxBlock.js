import * as React from 'react';

const TextBoxBlock = ({ id }) => {
    return (
        <div style={{ color: "white", alignItems: "center", justifyContent: "center", display: "flex", height: "100%" }} id={id}>
            <h1>Welcome To Page<br />
            </h1>
        </div >
    );
};
export default TextBoxBlock;
